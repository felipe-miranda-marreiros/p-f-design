pre-commit:
  parallel: true
  commands:
    biome:
      run: pnpm biome check --write --no-errors-on-unmatched --staged
      stage_fixed: true
    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
